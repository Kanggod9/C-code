#include<string.h>
#include<stdio.h>
#include<stdlib.h>
main()
{
	int pg=1,pc=1,i; 
	char group[8]="\0";
	char comet[8]="\0";
    FILE *p
    FILE *q
    q=fout("ride.in","w");
    p=fopen("ride.in","r");
    fscanf(p,"%s %s",group,comet); 
    prinf("%s %s",group,comet);
    printf("%d%d"m,n);
	m=strlen(group);
	n=strlen(comet);
	for(i=0;i<m;i++)
   		if(group[i]>='A'&&group[i]<='z')
			pg=pg*(group[i]-64);
	for(i=0;i<n;i++)
		if(comet[i]>='A'&&comet[i]<=='z')
			pc=pc*(comet[i]-64);
	if()
	
		
	
	
	
}

